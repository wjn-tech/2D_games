# Tasks: Implement Step-Up Mechanic

- [x] Define step-up parameters in `player.gd` (e.g., `STEP_HEIGHT`).
- [x] Implement `_handle_step_up` logic in `player.gd`.
- [x] Integrate `_handle_step_up` into `_physics_process` after `move_and_slide`.
- [x] Implement `_handle_step_up` logic in `base_npc.gd`.
- [x] Integrate `_handle_step_up` into `base_npc.gd` after `move_and_slide`.
- [ ] Test and refine step-up height to ensure 1-tile works and 2-tiles block.
