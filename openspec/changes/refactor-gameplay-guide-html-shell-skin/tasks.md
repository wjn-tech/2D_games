## 1. Baseline Freeze
- [x] 1.1 锁定 gameplay guide 现有逻辑契约（目录结构、页码、翻页规则、图像显隐、关闭行为）。
- [x] 1.2 锁定现有内容源契约（GuideSectionData 与目录树路径）并输出对照清单。
- [x] 1.3 锁定原生窗口回退可用性基线（可打开、可翻页、可关闭）。

## 2. Shell Style Mapping
- [x] 2.1 从 guide/HandbookWindow 提取视觉令牌（色板、像素边框、字体层级、按钮态、侧栏样式）。
- [x] 2.2 产出 gameplay guide HTML 壳静态资源目录与构建产物约束。
- [x] 2.3 将视觉令牌映射到引导窗口现有信息架构，确保不改章节与行为语义。

## 3. Bridge and Runtime Integration
- [x] 3.1 定义 gameplay guide Godot -> HTML 状态消息与字段白名单。
- [x] 3.2 定义 HTML -> Godot 意图消息（选页、翻页、关闭）与参数校验规则。
- [x] 3.3 接入 WebView-first 打开路径，桥接失败时自动回退原生窗口。
- [x] 3.4 记录异常分支（WebView 缺失、资源缺失、握手失败、运行时断链）的统一回退策略。

## 4. Parity Verification
- [x] 4.1 行为回归：目录点击、上一页/下一页、页码更新、图片有无切换、关闭窗口。
- [x] 4.2 内容回归：页面标题/路径/正文与现有实现逐项一致。
- [x] 4.3 稳定性回归：故障注入触发回退后仍可完整使用。
- [x] 4.4 平台门槛：桌面端通过；移动端确认降级策略可用。

## 5. Documentation
- [x] 5.1 补充壳边界文档（HTML 仅渲染与意图，不持有业务真相）。
- [x] 5.2 补充排障手册（桥接异常、资源缺失、焦点冲突、回退触发条件）。
- [x] 5.3 使用 strict 模式完成 OpenSpec 校验并留存结果。

## Dependency Notes
- 2.x 可与 3.1/3.2 并行。
- 4.x 依赖 1.x + 2.x + 3.x 完成。
- 5.x 与 4.x 可交错推进，最终以校验结果收敛。
