## 1. Baseline Freeze
- [x] 1.1 锁定现有逻辑契约：节点模板、连线规则、热键、编译/保存/测试入口、统计口径。
- [x] 1.2 锁定中文文案逐字基线（复用并更新 `chinese-copy-after.txt`）。
- [x] 1.3 锁定现有项目布局比例基线（左/中/右/顶栏的宽高占比与最小约束）。

## 2. Sample-Style Art Transplant (Desktop)
- [x] 2.1 从示例项目最终效果图提取美术令牌（配色、像素边框、扫描层、发光与交互反馈）。
- [x] 2.2 在 `ui/web/wand_editor_shell/` 产出可嵌入壳页面与资源，并完成主题映射。
- [x] 2.3 按现有项目比例重建界面排版，不改变布局比例策略。

## 3. Bridge Alignment (Logic Priority)
- [x] 3.1 定义 Godot -> Web 状态消息，覆盖渲染所需字段但不引入新逻辑语义。
- [x] 3.2 定义 Web -> Godot 意图消息，所有意图必须走现有逻辑处理链。
- [x] 3.3 在冲突处理策略中明确“示例主题让位于现有逻辑”，并记录分支规则。
- [x] 3.4 保留桥接失败自动回退原生编辑器与状态保全。

## 4. Parity Validation
- [ ] 4.1 行为回归：拖拽、连线、缩放、平移、Delete/Backspace、Esc、模式切换、测试、保存、切换法杖。
- [ ] 4.2 逻辑一致性：结果与原生编辑器一致，冲突样例按“现有逻辑优先”通过。
- [ ] 4.3 文案一致性：中文逐字 diff 为 0。
- [ ] 4.4 布局比例一致性：按基线占比检查通过。

## 5. Documentation
- [x] 5.1 更新 `verification.md`，记录“完整移植美术 + 逻辑不变 + 比例保持”的证据链。
- [x] 5.2 补充桥接协议与冲突处理说明（消息示例、回退条件、优先级规则）。

## Dependency Notes
- 2.x 与 3.x 可并行预研，但 4.x 依赖 1.x + 2.x + 3.x 完成。
- 5.x 与 4.x 可交错推进，最终以验证结果收敛。
