# Refine Wand Icons Proposal

## Goal
Fix visual inconsistency where undecorated wands show the Godot robot icon.

## Solution
Introduce `wand_base.svg` and update fallback logic in `WandTextureGenerator` or `Player` script.
